[Unit]
Description=SwayOSD server
PartOf=graphical-session.target
After=graphical-session.target swayosd-libinput-backend.service

[Service]
Type=simple
ExecStart=/bin/sh -c 'swayosd-server -s %h/.config/sway/osd.css'
Restart=on-failure

[Install]
WantedBy=graphical-session.target
